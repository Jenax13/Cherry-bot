module.exports = {
  env: {
    node: true,
    es2023: true,
    commonjs: true
  },
  parserOptions: {
    ecmaVersion: 'latest'
  },
  extends: [
    'eslint:recommended'
  ],
  rules: {
    // Estilo de comillas
    'quotes': ['error', 'single', { avoidEscape: true }],

    // Punto y coma obligatorio
    'semi': ['error', 'always'],

    // Indentación de 2 espacios
    'indent': ['error', 2],

    // Espacio antes de llaves
    'space-before-blocks': ['error', 'always'],

    // No usar var
    'no-var': 'error',

    // Preferir const cuando no se reasigna
    'prefer-const': 'warn',

    // No console.log en producción (warning)
    'no-console': 'off',

    // No dejar variables sin usar
    'no-unused-vars': ['warn', { args: 'none' }],

    // Funciones flecha preferidas donde tenga sentido
    'prefer-arrow-callback': 'warn'
  }
}
